apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: screensprout

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml

images:
  - name: screen-sprout-web
    newName: ghcr.io/screensprout/web
    newTag: latest
