apiVersion: v1
kind: Service
metadata:
  name: screen-sprout-api
  namespace: screensprout
  labels:
    app: screen-sprout-api
    tier: backend
spec:
  type: ClusterIP
  selector:
    app: screen-sprout-api
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
